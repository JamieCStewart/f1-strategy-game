services:
  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1

  web:
    build:
      context: web
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    depends_on:
      - api
